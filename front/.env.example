# Dev: leave empty — Vite proxies /api to localhost:3000
# Production (GitHub Pages): full backend URL, e.g. https://mci-api.onrender.com
VITE_API_URL=

# Set only for local preview of GitHub Pages build (CI sets this automatically)
# VITE_BASE_PATH=/mci/
